name: Hello World
on:
  push:
    branches:
      - main
jobs:
  Echo:
    name: Echo text

    runs-on: ubuntu-latest

    steps:
      
    - uses: actions/checkout@v4

    - run: echo "Hello, world!"


